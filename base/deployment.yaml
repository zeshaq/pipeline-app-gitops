apiVersion: apps/v1
kind: Deployment
metadata:
  name: pipeline-app-final
  labels:
    app: pipeline-app-final
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pipeline-app-final
  template:
    metadata:
      labels:
        app: pipeline-app-final
    spec:
      containers:
        - name: pipeline-app-final
          # Use the immutable SHA digest here instead of :latest
          image: ocp-real-registry-quay-openshift-operators.apps.cluster-realip.zeteq.com/zahid/pipeline-app-final@sha256:41355bf026883713c28a63dad93acbb8c6a046cfa5b6d0a601d6a9932032d7e1
          ports:
            - containerPort: 8080
          imagePullPolicy: Always
